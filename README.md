# RankIt
> A website to rank practically anything in an ELO style ranking system.

# Dev
```bash
# Install codegen tools (sqlc)
make setup
# Run postgres
docker compose up -d
# Run migrations
make migrate
```